---
layout: base.njk
---

<article>
  <header style="margin-bottom: 2rem; padding-bottom: 1.5rem; border-bottom: 1px solid #dcdcdc;">
    <h1 style="font-size: 2rem; margin-bottom: 0.5rem;">{{ title }}</h1>
    {% if description %}
    <p class="lead" style="margin-bottom: 1rem;">{{ description }}</p>
    {% endif %}
    <div style="display: flex; gap: 1rem; flex-wrap: wrap; align-items: center; font-size: 0.9rem; color: #555;">
      {% if date %}
      <time datetime="{{ date | dateIso }}">{{ date | dateReadable }}</time>
      {% endif %}
      {% if tags %}
      <div style="display: flex; gap: 0.5rem; flex-wrap: wrap;">
        {% for tag in tags %}
        <span style="background: #f0f6ff; border: 1px solid #d6e7ff; padding: 0.25rem 0.75rem; border-radius: 4px; font-size: 0.85rem; color: #004a7c;">{{ tag }}</span>
        {% endfor %}
      </div>
      {% endif %}
    </div>
  </header>

  <div class="post-content">
    {{ content | safe }}
  </div>

  <footer style="margin-top: 3rem; padding-top: 1.5rem; border-top: 1px solid #dcdcdc;">
    <div style="display: flex; justify-content: space-between; align-items: center; flex-wrap: wrap; gap: 1rem;">
      <div>
        <p style="color: #555; font-size: 0.9rem; margin: 0;">Written by Levi Huff</p>
      </div>
      <div>
        <a href="/blog/" style="text-decoration: none; font-weight: 600;">← Back to Blog</a>
      </div>
    </div>
  </footer>
</article>
